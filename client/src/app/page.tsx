import type { Metadata } from "next";
import { PageShell } from "@/components/common/PageShell";

export const metadata: Metadata = { title: "Workforce, Sales & Business Execution" };

export default function Page() {
  return <PageShell title="Workforce, Sales & Business Execution" />;
}
