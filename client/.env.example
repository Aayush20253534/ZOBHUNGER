# Use "mock" for frontend-only preview data or "api" for the real Phase 1 backend.
NEXT_PUBLIC_DATA_MODE=api

# Express API base URL. Keep /api/v1 in the value.
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
