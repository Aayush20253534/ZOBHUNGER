// TODO: initialise Prisma with @prisma/adapter-pg and DATABASE_URL.
// Run npm run db:generate first, then import the generated PrismaClient here.
// Keep database calls in the module repository files.
export {};
